import aurich, { size as aurichSize } from './aurich'
import beemoor, { size as beemoorSize } from './beemoor'
import bremen, { size as bremenSize } from './bremen'
import dornum, { size as dornumSize } from './dornum'
import emden, { size as emdenSize } from './emden'
import esens, { size as esensSize } from './esens'
import hage, { size as hageSize } from './hage'
import leer, { size as leerSize } from './leer'
import norden, { size as nordenSize } from './norden'

export const size = {
  aurich: aurichSize,
  beemoor: beemoorSize,
  bremen: bremenSize,
  dornum: dornumSize,
  emden: emdenSize,
  esens: esensSize,
  hage: hageSize,
  leer: leerSize,
  norden: nordenSize,
}

export default {
  aurich,
  beemoor,
  bremen,
  dornum,
  emden,
  esens,
  hage,
  leer,
  norden,
}
